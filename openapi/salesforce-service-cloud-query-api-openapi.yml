openapi: 3.2.0
info:
  title: Salesforce Service Cloud REST Query API
  description: RESTful API for managing customer service operations in Salesforce Service Cloud, including cases, contacts, accounts, knowledge articles, and other service-related objects via the sObject framework.
  version: '59.0'
  contact:
    name: Salesforce Developer Support
    url: https://developer.salesforce.com/support
  termsOfService: https://www.salesforce.com/company/legal/agreements/
servers:
- url: https://{instance}.salesforce.com/services/data/v59.0
  description: Salesforce Production Instance
  variables:
    instance:
      default: yourInstance
      description: Your Salesforce instance identifier
security:
- oauth2: []
- bearerAuth: []
tags:
- name: Query
  description: SOQL query operations
paths:
  /query:
    get:
      operationId: executeQuery
      summary: Salesforce Service Cloud Execute a SOQL query
      description: Executes a SOQL query against service cloud objects and returns matching records.
      tags:
      - Query
      parameters:
      - name: q
        in: query
        required: true
        description: SOQL query string
        schema:
          type: string
          example: SELECT Id, Subject, Status FROM Case WHERE Status = 'New'
      responses:
        '200':
          description: Query results
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/QueryResult'
        '400':
          description: Invalid SOQL query
components:
  schemas:
    QueryResult:
      type: object
      properties:
        totalSize:
          type: integer
          description: Total number of matching records
        done:
          type: boolean
          description: Whether all results are returned
        records:
          type: array
          items:
            type: object
          description: Array of matching records
  securitySchemes:
    oauth2:
      type: oauth2
      flows:
        authorizationCode:
          authorizationUrl: https://login.salesforce.com/services/oauth2/authorize
          tokenUrl: https://login.salesforce.com/services/oauth2/token
          scopes:
            api: Access Salesforce REST API
            full: Full access
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: OAuth2
externalDocs:
  description: REST API Developer Guide
  url: https://developer.salesforce.com/docs/atlas.en-us.api_rest.meta/api_rest/
